const Number = [10, 11, 12, 13, 14, 15];

let text = "";
for (let i = 0; i < Number.length; i++) {
  console.log(Number[i] * 2);
}

const device = {
  model: "keyboard",
  price: $250,
  size: "300",
  color: "black",
};
const { mode, price, sixze, color } = device;
console.log(color);
